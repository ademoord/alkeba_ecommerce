<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="action_report_saleorder_waiting_payment" model="ir.actions.report">
            <field name="name">Waiting Payment Invoice</field>
            <field name="model">sale.order</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">theme_alkeba.report_waiting_payment</field>
            <field name="report_file">theme_alkeba.report_waiting_payment</field>
            <field name="print_report_name">(object.state in ('draft', 'sent') and 'Quotation - %s' % (object.name)) or 'Order - %s' % (object.name)</field>
            <field name="binding_model_id" ref="model_sale_order"/>
            <field name="binding_type">report</field>
        </record>

        <record id="action_report_saleorder_payment_success" model="ir.actions.report">
            <field name="name">Payment Success Invoice</field>
            <field name="model">sale.order</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">theme_alkeba.report_payment_success</field>
            <field name="report_file">theme_alkeba.report_payment_success</field>
            <field name="print_report_name">(object.state in ('draft', 'sent') and 'Quotation - %s' % (object.name)) or 'Order - %s' % (object.name)</field>
            <field name="binding_model_id" ref="model_sale_order"/>
            <field name="binding_type">report</field>
        </record>

        <record id="action_report_saleorder_order_confirmation_va_payment" model="ir.actions.report">
            <field name="name">Payment VA Invoice</field>
            <field name="model">sale.order</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">theme_alkeba.report_order_confirmation_va_payment</field>
            <field name="report_file">theme_alkeba.report_order_confirmation_va_payment</field>
            <field name="print_report_name">(object.state in ('draft', 'sent') and 'Quotation - %s' % (object.name)) or 'Order - %s' % (object.name)</field>
            <field name="binding_model_id" ref="model_sale_order"/>
            <field name="binding_type">report</field>
        </record>

        <record id="action_report_order_confirmation_bayartoko_payment" model="ir.actions.report">
            <field name="name">Payment Bayar Ditoko Invoice</field>
            <field name="model">sale.order</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">theme_alkeba.report_order_confirmation_bayartoko_payment</field>
            <field name="report_file">theme_alkeba.report_order_confirmation_bayartoko_payment</field>
            <field name="print_report_name">(object.state in ('draft', 'sent') and 'Quotation - %s' % (object.name)) or 'Order - %s' % (object.name)</field>
            <field name="binding_model_id" ref="model_sale_order"/>
            <field name="binding_type">report</field>
        </record>

        <record id="action_report_saleorder_order_process" model="ir.actions.report">
            <field name="name">Order Process Invoice</field>
            <field name="model">sale.order</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">theme_alkeba.report_order_process</field>
            <field name="report_file">theme_alkeba.report_order_process</field>
            <field name="print_report_name">(object.state in ('draft', 'sent') and 'Quotation - %s' % (object.name)) or 'Order - %s' % (object.name)</field>
            <field name="binding_model_id" ref="model_sale_order"/>
            <field name="binding_type">report</field>
        </record>

        <record id="action_report_saleorder_order_send" model="ir.actions.report">
            <field name="name">Order Send Invoice</field>
            <field name="model">sale.order</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">theme_alkeba.report_order_send</field>
            <field name="report_file">theme_alkeba.report_order_send</field>
            <field name="print_report_name">(object.state in ('draft', 'sent') and 'Quotation - %s' % (object.name)) or 'Order - %s' % (object.name)</field>
            <field name="binding_model_id" ref="model_sale_order"/>
            <field name="binding_type">report</field>
        </record>

        <record id="action_report_saleorder_order_done" model="ir.actions.report">
            <field name="name">Order Done Invoice</field>
            <field name="model">sale.order</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">theme_alkeba.report_order_done</field>
            <field name="report_file">theme_alkeba.report_order_done</field>
            <field name="print_report_name">(object.state in ('draft', 'sent') and 'Quotation - %s' % (object.name)) or 'Order - %s' % (object.name)</field>
            <field name="binding_model_id" ref="model_sale_order"/>
            <field name="binding_type">report</field>
        </record>
    </data>
</odoo>